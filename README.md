Lesara Python developer task
================================

To run the jupyter notebook and the flask application, simply run `docker-compose up`.

To use the jupyter notebook, click on the link in the jupyter container log.
